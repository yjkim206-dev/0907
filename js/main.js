const nav=document.querySelector('.site-nav'),menu=document.querySelector('.menu-toggle');if(menu&&nav)menu.addEventListener('click',()=>{const o=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',o)});document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const m=f.querySelector('.form-message');if(m)m.textContent=f.dataset.message||'처리되었습니다.'}));
